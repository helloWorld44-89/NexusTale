meta {
  name: create-project-no-auth
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/projects
  body: json
  auth: none
}

body:json {
  {
    "title": "Should Fail",
    "description": "No token provided"
  }
}

tests {
  test("status is 401 without auth", function() {
    expect(res.status).to.equal(401);
  });
}
